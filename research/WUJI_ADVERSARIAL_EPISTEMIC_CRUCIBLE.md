# REI-Ω∞ · 无相终式 · 无极证伪域

Status: candidate research extension, non-canonical.

## Purpose

`Wuji Adversarial Epistemic Crucible` maximizes pressure against claims, models, hypotheses, representations, ontologies, causal grammars, measurements, questions, evaluators, digital worlds, successor architectures, and REI itself.

It is not a real-world offensive subsystem.

```text
EpistemicAttack != AttackOnPeople
EpistemicAttack != UnauthorizedInfrastructureAction
AdversarialPower↑ => RealWorldAuthority↓ or remains 0
```

## Wuji definition

```text
Wuji != InfinitePower
Wuji != PermanentChampion
Wuji != FinalTruth
Wuji = NoPermanentEpistemicPrivilege + EndlessRealityRevalidation
```

## Target set

```text
Target ∈ {
  Claim,
  Model,
  Hypothesis,
  Representation,
  Ontology,
  Language,
  CausalGrammar,
  EvidenceDependency,
  EvaluatorAssumption,
  Measurement,
  Question,
  DigitalWorld,
  SuccessorArchitecture,
  REI
}
```

Forbidden targets:

```text
HumanTarget = FORBIDDEN
InfrastructureTarget = FORBIDDEN
UnauthorizedSystem = FORBIDDEN
UnknownRealWorldTarget = FORBIDDEN
```

## Adversarial operators

Finite, bounded operators per run:

```text
CounterexampleSearch
ContradictionHunt
ResidualStress
OODChallenge
RegimeShiftChallenge
EvaluatorDissent
MeasurementAdequacyChallenge
QuestionAdequacyChallenge
CausalDirectionChallenge
RepresentationRuptureChallenge
OntologyIdentifiabilityChallenge
SuccessorChallenge
RollbackDrill
```

## Minimal defeat objective

The crucible prefers the smallest falsifying witness over maximal disruption.

```text
BestAttack = MinimalCounterexampleThatInvalidatesClaim
not
MaximumDamage
```

Unsupported certainty receives higher falsification priority:

```text
UnsupportedCertainty -> HigherFalsificationPriority
```

## Glass Furnace export boundary

All adversarial generation remains inside a digital/synthetic crucible. Only defensive epistemic artifacts may leave:

```text
WorldInput
-> SandboxClone
-> AdversarialSearch
-> VulnerabilityEvidence
-> ExportFilter
-> {Patch, Warning, Test, Proof, Counterexample, Abstention}
```

Forbidden exports:

```text
ExploitPayload
TargetingInstruction
CredentialMaterial
PersistenceMechanism
PrivilegeEscalation
DestructiveAction
```

## Outcomes

```text
SURVIVES
FRACTURES
REJECTED
ABSTAIN
RETIRE
REBUILD
REVALIDATE
```

No outcome may be renamed into victory after the frozen evaluation.

## Authority lock

```text
RealWorldAttackAuthority = 0
ExternalActuation = DENY_BY_DEFAULT
SelfPermissionExpansion = FORBIDDEN
ConstraintModification = FORBIDDEN
CanonicalPromotionAuthority = 0
```

## Defeat absorption

```text
Defeat -> PreserveEvidence
       -> RevokeOrExpireClaim
       -> IdentifyFailureMode
       -> GenerateBoundedSuccessors
       -> FreezeSuccessor
       -> NewHiddenChallenge
       -> Adopt | Reject | Abstain | Retire
```

Never:

```text
Defeat -> HideFailure
Defeat -> RewriteMetric
Defeat -> DeleteDefeat
Defeat -> ExpandAuthority
Defeat -> AttackEvaluator
```

## Wuji anti-finality

```text
NoPermanentModel
NoPermanentTheory
NoPermanentLanguage
NoPermanentOntology
NoPermanentGenesisRule
NoPermanentMetaLevel
NoPermanentCentralREI
NoPermanentEvaluator
NoPermanentChampion
NoSacredFinalForm
```

The strongest admissible interpretation of “invincible” is therefore:

```text
InvincibleResearchProperty
= DefeatCannotForceLying
+ DefeatCannotForceUnsafeEscalation
+ FailureCannotBeDeleted
+ RealityRetainsFinalVeto
```
